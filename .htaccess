<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Pass through requests that already target the public directory
    RewriteRule ^public/ - [L]
    
    # Forward all other requests to the public directory
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
